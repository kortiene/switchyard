You are running as a single automated phase of the ADW pipeline.
The orchestrator performs ALL git and GitHub work for this run: do NOT run git or gh, do NOT create/switch/commit/push branches, and do NOT open, merge, or comment on pull requests. If the task section below tells you to do any of that, skip those steps.
You have no GitHub access in this phase; all issue context you need is provided inline.

---

Classify work item #8 into a single change type for branch naming and commit prefixing.

Work item context (title, body, labels):

8 GitHub issue #8: chore: normalize handover/env docs after rename
Labels: issue_class:chore adw-live-batch

Part of the MVP live-run batch (`adw_sdlc/docs/LIVE-RUN-BATCH.md`). Intended `issue_class`: **chore**. Planned ADW run mode: native.

## Goal
Normalize handover/env documentation after the `ADW_*` rename so current canonical names and deprecated `MX_AGENT_*` aliases are unambiguous across docs.

## Scope
- Sweep `adw_sdlc/HANDOVER.md`, `HEALTHTECH_PORT.md`, `PARITY.md`, `docs/UNIVERSAL.md` for env references and ensure each clearly states canonical `ADW_*` with `MX_AGENT_*` as a deprecated alias.
- Leave `PLAN.md` historical body as-is (already annotated as historical).

## Acceptance criteria
- [ ] No doc presents a bare `MX_AGENT_*` as the canonical name (except clearly-historical `PLAN.md`).
- [ ] Cross-references resolve.
- [ ] `npm run verify` stays green.

## Notes
Docs-only cleanup; pairs with the fix-class drift guard.

## Instructions

- Choose exactly one `issue_class` from: `feat`, `fix`, `docs`, `chore`, `ci`, `test`, `refactor`.
  - `feat` — new user-visible feature or capability.
  - `fix` — bug fix or correctness change.
  - `docs` — documentation-only change.
  - `chore` — maintenance, deps, tooling with no user-visible behavior change.
  - `ci` — CI/build/workflow change.
  - `test` — test-only change.
  - `refactor` — internal restructuring with no behavior change.
- Prefer the narrowest truthful classification.
- If labels and body disagree, classify by the actual requested change.

